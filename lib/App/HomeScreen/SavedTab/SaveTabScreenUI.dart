import 'package:flutter/material.dart';

class SaveTabScreenUI extends StatelessWidget {
  const SaveTabScreenUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
